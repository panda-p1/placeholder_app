import 'package:flutter/material.dart';

class EntrySettings {
  final Color backgroundColor;
  final double opacity;
  final double radius;
  const EntrySettings({this.backgroundColor = const Color.fromRGBO(0, 0, 0, 0.38,), this.opacity = 0.5, this.radius = 20.0, });
}
